apiVersion: apps/v1
kind: Deployment
metadata:
  name: pruebaangular
  labels:
    app: lundero-devops
spec:
  selector:
    matchLabels:
      app: lundero-devops
  replicas: 3
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: lundero-devops
    spec:
      containers:
      - name: pruebaangular
        image: 152512582646.dkr.ecr.us-east-2.amazonaws.com/repo-app-sebas:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
apiVersion: v1
kind: Service
metadata:
  name: pruebaangular
  labels:
    app: lundero-devops
spec:
  selector:
    app: lundero-devops
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
    name: lundero-devops